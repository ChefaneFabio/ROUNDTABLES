import { Router } from 'express'

const router = Router()

// Placeholder for participant management endpoints
router.get('/', (req, res) => {
  res.json({ success: true, data: [], message: 'Participant endpoints coming soon' })
})

export default router