import { Router } from 'express'

const router = Router()

// Placeholder for notification management endpoints  
router.get('/', (req, res) => {
  res.json({ success: true, data: [], message: 'Notification endpoints coming soon' })
})

export default router